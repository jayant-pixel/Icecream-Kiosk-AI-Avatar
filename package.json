{
  "name": "icecream-kiosk-ai-avatar",
  "private": true,
  "version": "1.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "cd frontend && npx cross-env NEXT_DISABLE_TURBOPACK=1 next dev",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace frontend",
    "lint": "npm run lint --workspace frontend",
    "format": "npm run lint --workspace frontend -- --fix",
    "agent": "python agents/avatar_anam.py start"
  }
}